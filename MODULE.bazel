# =============================================================================
# STUD.io ControlRoom — Bazel module (Phase 0: sh_test wrappers)
#
# Phase 1 will add rules_python, rules_js, rules_ts for native hermetic rules.
# =============================================================================

module(
    name = "controlroom",
    version = "0.0.0",
    compatibility_level = 1,
)

bazel_dep(name = "buildifier_prebuilt", version = "7.3.1")
