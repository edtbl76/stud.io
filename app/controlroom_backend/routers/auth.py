"""
Auth router — placeholder. JWT implementation pending users table.
"""
from fastapi import APIRouter

router = APIRouter()


# TODO: implement JWT auth
# Requires: users table, password hashing (bcrypt), python-jose
