# Phase 0: sh_test wrappers around existing scripts.
# These targets are hermetic (no live infrastructure required) and are
# included in //:unit and in bazel test //...

sh_test(
    name = "tsc",
    srcs = ["//tools/bazel:tsc.sh"],
    tags = ["local"],
)

sh_test(
    name = "jest",
    srcs = ["//tools/bazel:jest.sh"],
    tags = ["local"],
)
