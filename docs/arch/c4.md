# C4 Architecture — STUD.io

All diagrams use [Mermaid C4](https://mermaid.js.org/syntax/c4.html) and render in GitHub, VS Code, and any Markdown viewer with Mermaid support.

**Update policy:** See `CLAUDE.md` — Architecture diagrams section. Any change to a service, container, module, or significant component requires updating the relevant diagram below before the task is considered done.

---

## Level 1 — System Context

```mermaid
C4Context
    title STUD.io — System Context

    Person(owner, "Studio Owner", "Manages catalog, gear, and admin operations. Has admin role.")
    Person(user, "User", "Browses and searches the catalog. Read-only.")
    Person(dev, "Developer", "Operates the local stack via Roadie CLI.")

    System(studio, "STUD.io", "Private music production management platform. Catalog, gear inventory, global search, audit logging, and admin ops.")
    System_Ext(google, "Google OAuth 2.0", "Provides user identity for Google Sign-In.")
    System_Ext(docker, "Docker Daemon", "Local container engine managed by Roadie CLI.")

    Rel(owner, studio, "Manages catalog and admin operations")
    Rel(user, studio, "Browses and searches catalog")
    Rel(dev, studio, "Operates via Roadie CLI")
    Rel(studio, google, "Validates Google ID tokens for sign-in")
    Rel(dev, docker, "roadie start/stop/build via docker compose")
```

---

## Level 2 — Containers

```mermaid
C4Container
    title STUD.io — Container Diagram

    Person(owner, "Studio Owner")
    Person(user, "User")
    Person(dev, "Developer")

    System_Boundary(studio, "STUD.io") {
        Container(nginx, "Nginx", "nginx:alpine", "TLS termination and reverse proxy. :2112 → frontend, :5150 → backend.")
        Container(frontend, "Next.js Frontend", "Next.js 16 / React 19 / TypeScript", "BFF pattern. JWT stored in httpOnly cookie. All API calls proxied server-side.")
        Container(backend, "FastAPI Backend", "Python 3.12 / FastAPI / asyncpg", "REST API. CRUD for all entities, global search, audit log, admin ops, GearList proxy.")
        Container(gearlist, "GearList Service", "Go 1.26 / net/http / pgx", "Gear inventory, gear types, maintenance log, photo upload. Internal service only.")
        ContainerDb(db, "PostgreSQL", "pgvector/pg17", "Primary data store. masterdb (prod) + masterdb_test. Soft deletes + audit_log.")
        Container(minio, "MinIO", "MinIO (S3-compatible)", "Object storage for gear photos. Bucket: studio-photos. Internal access only.")
    }

    System_Ext(google, "Google OAuth 2.0")
    System_Ext(docker, "Docker Daemon")

    Container(roadie, "Roadie CLI", "Go 1.26 / Cobra", "Developer operations. start/stop/build/test/release/doctor.")

    Rel(owner, nginx, "HTTPS :2112 / :5150")
    Rel(user, nginx, "HTTPS :2112")
    Rel(nginx, frontend, "Proxy :2112")
    Rel(nginx, backend, "Proxy :5150")
    Rel(frontend, backend, "REST — JWT attached by BFF proxy")
    Rel(backend, db, "asyncpg connection pool")
    Rel(backend, gearlist, "HTTP proxy — X-User / X-Role headers")
    Rel(backend, google, "Validate Google ID tokens")
    Rel(gearlist, db, "pgx connection pool")
    Rel(gearlist, minio, "minio-go — PutObject / RemoveObject")
    Rel(roadie, docker, "docker compose up / down / ps / exec")
    Rel(dev, roadie, "CLI commands")
```

---

## Level 3 — Frontend Components

```mermaid
C4Component
    title Next.js Frontend — Components

    Container_Boundary(frontend, "Next.js Frontend") {
        Component(topbar, "TopBar", "components/layout/TopBar.tsx", "Fixed global search bar on all non-login pages. Submits to /search.")
        Component(pages, "Pages", "app/", "Five modules: Home, Search, ControlRoom, Studio Management, GearList. Each has a layout with Sidebar/UsersSidebar/GearListSidebar.")
        Component(tablepage, "TablePage Framework", "TablePage.tsx + DataTable.tsx + useTableData.ts", "Generic CRUD page used by all 18 data tables. Pagination, filtering, sorting, session state, bulk edit.")
        Component(modals, "Modal System", "RecordModal.tsx + components/tables/", "RecordModal shell + 9 domain modals. View/edit/history/delete lifecycle. GearModal adds photo upload and pickup config.")
        Component(authctx, "Auth Context", "lib/auth.tsx", "Session check on mount. Redirects unauthenticated users to /login.")
        Component(apiclient, "API Client", "lib/api.ts", "Typed fetch wrapper. All calls to relative /api/... paths. No Authorization header set client-side.")
        Component(bffproxy, "BFF Catch-All Proxy", "app/api/[...path]/route.ts", "Reads httpOnly cookie, attaches Bearer token, forwards to FastAPI.")
        Component(authroutes, "Auth API Routes", "app/api/auth/", "Login (username/password + Google SSO), logout. Sets/clears httpOnly cookie.")
    }

    ContainerDb(db, "PostgreSQL", "pgvector/pg17", "")
    Container(backend, "FastAPI Backend", "", "")

    Rel(pages, topbar, "Rendered by LayoutShell on all non-login pages")
    Rel(pages, authctx, "Reads username and role")
    Rel(pages, tablepage, "All catalog/session/tools/config pages")
    Rel(tablepage, apiclient, "Fetch data, submit mutations")
    Rel(tablepage, modals, "Open on row click")
    Rel(modals, apiclient, "Save and delete records")
    Rel(authctx, authroutes, "login, loginGoogle, logout, /api/auth/me")
    Rel(apiclient, bffproxy, "Every fetch('/api/...')")
    Rel(bffproxy, backend, "Forwards with Authorization: Bearer")
    Rel(authroutes, backend, "POST /auth/token, /auth/google, GET /auth/me")
```

---

## Level 3 — FastAPI Backend Components

```mermaid
C4Component
    title FastAPI Backend — Components

    Container_Boundary(backend, "FastAPI Backend") {
        Component(appcore, "App Core", "main.py", "Entry point. Registers CORS, lifespan, asyncpg pool, seeds admin account, mounts all routers.")
        Component(authrouter, "Auth Router", "routers/auth.py", "JWT (HS256) + Google OAuth. Exposes get_current_user and require_admin FastAPI dependencies.")
        Component(catalogrouters, "Catalog Routers", "routers/brands|models|effects|instruments|libraries|workstations.py", "CRUD + paginated list + audit history for 6 entity types.")
        Component(toolsrouter, "Tools Router", "routers/tools.py", "CRUD for 5 tool categories (admin/composition/measurement/reference/workflow).")
        Component(configrouter, "Config Router", "routers/config.py", "CRUD for 8 lookup tables. Slug-to-table mapping. Referential integrity on delete.")
        Component(searchrouter, "Search Router", "routers/search.py", "Full-text search via websearch_to_tsquery across 11 catalog views.")
        Component(usersrouter, "Users Router", "routers/users.py", "User CRUD, role management, Google account linking.")
        Component(adminrouters, "Admin Routers", "routers/change_review|admin_stats|backup_ops|import_export.py", "Audit review + undo, stats, pg_dump backup, XLSX import/export.")
        Component(gearlistrouter, "GearList Proxy", "routers/gearlist.py", "Proxies /gearlist/* to Go service. Strips JWT, injects X-User/X-Role.")
        Component(crudlib, "CRUD Library", "routers/_crud_ops.py", "Shared: list_entities, get_entity, delete_entity (soft), get_history, log_audit.")
        Component(dbpool, "DB Pool", "database.py", "asyncpg pool (min=2, max=10). JSON codec. get_conn() FastAPI dependency.")
    }

    ContainerDb(db, "PostgreSQL", "", "")
    System_Ext(google, "Google OAuth 2.0", "")
    Container(gearlist, "GearList Service", "", "")

    Rel(appcore, authrouter, "Mounts at /auth")
    Rel(appcore, catalogrouters, "Mounts at /studio/catalog + /studio/session")
    Rel(appcore, toolsrouter, "Mounts at /studio/tools/{category}")
    Rel(appcore, configrouter, "Mounts at /studio/config/{slug}")
    Rel(appcore, searchrouter, "Mounts at /search")
    Rel(appcore, usersrouter, "Mounts at /users")
    Rel(appcore, adminrouters, "Mounts at /admin/*")
    Rel(appcore, gearlistrouter, "Mounts at /gearlist")
    Rel(catalogrouters, crudlib, "list, get, delete, history")
    Rel(toolsrouter, crudlib, "list, get, delete, history")
    Rel(configrouter, crudlib, "list, get, delete")
    Rel(dbpool, db, "asyncpg connection pool")
    Rel(authrouter, google, "Validate Google ID tokens")
    Rel(gearlistrouter, gearlist, "HTTP proxy")
```

---

## Level 3 — GearList Service Components

```mermaid
C4Component
    title GearList Service — Components

    Container_Boundary(gearlist, "GearList Service") {
        Component(server, "Server", "internal/server/server.go", "HTTP router, middleware, health check registration.")
        Component(gearhandler, "Gear Handler", "internal/gear/handler.go", "CRUD for gear items. Paginated list with name/type filters. Photo upload dispatch.")
        Component(geartypeshandler, "GearTypes Handler", "internal/geartypes/handler.go", "CRUD for gear_types lookup table.")
        Component(maintenancehandler, "Maintenance Handler", "internal/maintenance/handler.go", "Append-only maintenance log per gear item.")
        Component(photouploader, "Photo Uploader", "internal/gear/photo.go", "Validates Content-Type + size. Uploads to MinIO. Cleans up orphans on DB write failure.")
        Component(auditstore, "Audit Store", "internal/store/audit.go", "Writes CREATE/UPDATE/DELETE entries to audit_log.")
        Component(config, "Config", "internal/config/config.go", "Loads APP_PORT, DB credentials, MinIO credentials from env.")
    }

    ContainerDb(db, "PostgreSQL", "", "")
    Container(minio, "MinIO", "", "")
    Container(backend, "FastAPI Backend", "", "")

    Rel(backend, server, "HTTP — X-User / X-Role headers")
    Rel(server, gearhandler, "Routes /gear/*")
    Rel(server, geartypeshandler, "Routes /gear-types/*")
    Rel(server, maintenancehandler, "Routes /gear/{id}/maintenance")
    Rel(gearhandler, photouploader, "Delegates photo upload")
    Rel(gearhandler, auditstore, "Log all writes")
    Rel(geartypeshandler, auditstore, "Log all writes")
    Rel(photouploader, minio, "PutObject / RemoveObject")
    Rel(gearhandler, db, "pgx — gear + gear_view")
    Rel(geartypeshandler, db, "pgx — gear_types")
    Rel(maintenancehandler, db, "pgx — gear_maintenance_log")
    Rel(auditstore, db, "pgx — audit_log")
```

---

## Level 3 — Roadie CLI Components

```mermaid
C4Component
    title Roadie CLI — Components

    Container_Boundary(roadie, "Roadie CLI") {
        Component(cli, "CLI Entry Point", "cmd/roadie/main.go", "Cobra root. --verbose flag. Delegates to subcommand groups.")
        Component(stackcmds, "Stack Commands", "internal/commands/stack.go", "start, stop, restart, status.")
        Component(buildcmds, "Build Commands", "internal/commands/build.go", "build, release. Rebuilds images, applies schema, runs test suites.")
        Component(dbcmds, "DB Commands", "internal/commands/db.go", "db init — first-time production schema setup with confirmation gate.")
        Component(testcmds, "Test Commands", "internal/commands/test.go", "test unit/e2e/scan/perf/pbt/full.")
        Component(doctorcmds, "Doctor Commands", "internal/commands/doctor.go + doctor_fix.go", "Prerequisite checks. fix secrets and fix woodpecker-agents.")
        Component(stackmgr, "Stack Manager", "internal/stack/manager.go", "Orchestrates start/stop/health checks. Polls every 2s up to 5 min.")
        Component(pipeline, "Pipeline Engine", "internal/pipeline/", "ToolStep + Pipeline. Fatal-sequential and collect execution modes. LabelWriter output prefixing.")
        Component(configloader, "Config Loader", "internal/config/", "Reads and validates roadie.yml.")
        Component(providers, "Providers", "internal/providers/", "DockerProvider (compose), PostgresProvider (pg_isready/psql), HTTPChecker.")
    }

    System_Ext(docker, "Docker Daemon", "")

    Rel(cli, stackcmds, "Registers and delegates")
    Rel(cli, buildcmds, "Registers and delegates")
    Rel(cli, dbcmds, "Registers and delegates")
    Rel(cli, testcmds, "Registers and delegates")
    Rel(cli, doctorcmds, "Registers and delegates")
    Rel(stackcmds, stackmgr, "Start / Stop / Status")
    Rel(buildcmds, pipeline, "Run test suites")
    Rel(buildcmds, providers, "Apply schema to test DBs")
    Rel(stackmgr, providers, "Health checks + container lifecycle")
    Rel(providers, docker, "docker compose up/down/ps/exec/pg_isready")
    Rel(stackcmds, configloader, "Load roadie.yml")
    Rel(buildcmds, configloader, "Load roadie.yml")
    Rel(dbcmds, configloader, "Load roadie.yml")
```

---

## Level 4 — BFF JWT Flow

```mermaid
sequenceDiagram
    participant Browser
    participant Nginx
    participant Frontend as Next.js Frontend (BFF)
    participant Backend as FastAPI Backend
    participant DB as PostgreSQL

    Browser->>Nginx: HTTPS GET /api/studio/effects (httpOnly cookie sent automatically)
    Nginx->>Frontend: HTTP proxy
    Frontend->>Backend: GET /studio/effects + Authorization: Bearer {token from cookie}
    Backend->>DB: SELECT * FROM effects_view WHERE ... LIMIT $1 OFFSET $2
    DB-->>Backend: rows
    Backend-->>Frontend: JSON
    Frontend-->>Browser: JSON (proxied)

    note over Browser,Frontend: JWT never touches browser JS — only the httpOnly cookie
```

---

## Level 4 — FastAPI Auth Dependency Chain

```mermaid
sequenceDiagram
    participant Request
    participant AppCore as FastAPI App Core
    participant AuthRouter as Auth Router
    participant DB as PostgreSQL
    participant CatalogRouter as Catalog Router
    participant CRUDLib as CRUD Library

    Request->>AppCore: PATCH /studio/effects/{id} (admin write)
    AppCore->>AuthRouter: Resolve require_admin dependency
    AuthRouter->>DB: SELECT username, role FROM users WHERE username = $1
    DB-->>AuthRouter: {username, role: "admin"}
    AuthRouter-->>AppCore: Dependency satisfied
    AppCore->>CatalogRouter: Route handler invoked
    CatalogRouter->>CRUDLib: update_entity(id, data)
    CRUDLib->>DB: UPDATE effects SET ... + INSERT INTO audit_log
```

---

## Level 4 — Roadie Start Flow

```mermaid
sequenceDiagram
    participant Dev as Developer
    participant CLI as Roadie CLI
    participant Config as Config Loader
    participant Manager as Stack Manager
    participant Docker as Docker Provider
    participant Daemon as Docker Daemon
    participant Postgres as Postgres Provider
    participant HTTP as HTTP Checker

    Dev->>CLI: roadie start [--dev]
    CLI->>Config: Load and validate roadie.yml
    CLI->>Manager: NewManager(providers)
    Manager->>Docker: ContainerProvider.Up
    Docker->>Daemon: docker compose up -d --remove-orphans
    loop Poll every 2s (up to 5 min)
        Manager->>Postgres: IsReady?
        Postgres->>Daemon: pg_isready -U studio -q
    end
    loop Poll every 2s
        Manager->>HTTP: IsReachable? (API + Frontend)
        HTTP->>Daemon: GET https://localhost:5150/health
    end
    Manager-->>Dev: All health checks passed — URLs printed
```

---

## Level 4 — Roadie Build Flow

```mermaid
sequenceDiagram
    participant Dev as Developer
    participant CLI as Roadie CLI
    participant Manager as Stack Manager
    participant Docker as Docker Daemon
    participant Postgres as Postgres Provider
    participant Pipeline as Pipeline Engine

    Dev->>CLI: roadie build [--dev] [--full]
    CLI->>Manager: Build — compose up --build --force-recreate
    Manager->>Docker: docker compose up --build --force-recreate
    CLI->>Postgres: Apply schema_files to each test database
    Postgres->>Docker: docker compose exec psql (pipe SQL via stdin)
    CLI->>Pipeline: RunParallel: tsc → jest → ruff → bandit → pytest → go-test
    Pipeline-->>Dev: PASS/FAIL summary table
```
